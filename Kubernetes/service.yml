apiVersion: v1
kind: Service
metadata:
  name: wallpaper-app
  labels:
    app: wallpaper-app
spec:
  type: NodePort
  ports:
  - port: 80
    targetPort: 80
    nodePort: 30007
  selector:
    app: wallpaper-app
